services:
  db:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_USER: care_clinic
      POSTGRES_PASSWORD: care_clinic
      POSTGRES_DB: care_clinic
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: